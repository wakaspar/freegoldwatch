<p id="notice"><%= notice %></p>

<!-- Dashboard -->

<div class="container">
  <div class="row">
    <div class="col s12">
      <ul class="tabs">
        <li class="tab col s3 active"><a href="#pending">Pending</a></li>
        <li class="tab col s3"><a class="active" href="#printing">Printing</a></li>
        <li class="tab col s3"><a href="#pickup">PickUp</a></li>
        <li class="tab col s3"><a href="#archive">Archive</a></li>
      </ul>
    </div>

    <!-- New Print Link -->
    <div class="row">
      <%= link_to 'New Print Order', new_print_path %>
    </div>

    <!-- Pending Orders -->
    <div id="pending" class="col s12">
      <h1>Pending</h1>
      <table>
        <thead>
          <tr>
            <th>Name</th>
            <th>Quantity</th>
            <th>Order state</th>
            <th>Contact email</th>
            <th>Contact phone</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @prints.each do |print| %>
            <% if print.order_state == 0 %>
              <tr>
                <td><%= print.name %></td>
                <td><%= print.quantity %></td>
                <td><%= print.order_state %></td>
                <td><%= print.contact_email %></td>
                <td><%= print.contact_phone %></td>
                <td><%= link_to 'Show', print %></td>
                <td><%= link_to 'Edit', edit_print_path(print) %></td>
                <td><%= link_to 'Destroy', print, method: :delete, data: { confirm: 'Are you sure?' } %></td>
                <td><%= link_to 'Next Stage', bump_up_path(print), method: :put %></td>
              </tr>
            <% end %>
          <% end %>
        </tbody>
      </table>
      <br>
    </div>

    <!-- Printing Orders -->
    <div id="printing" class="col s12">
      <h1>Printing</h1>
      <table>
        <thead>
          <tr>
            <th>Name</th>
            <th>Quantity</th>
            <th>Order state</th>
            <th>Contact email</th>
            <th>Contact phone</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @prints.each do |print| %>
            <% if print.order_state == 1 %>
              <tr>
                <td><%= print.name %></td>
                <td><%= print.quantity %></td>
                <td><%= print.order_state %></td>
                <td><%= print.contact_email %></td>
                <td><%= print.contact_phone %></td>
                <td><%= link_to 'Show', print %></td>
                <td><%= link_to 'Edit', edit_print_path(print) %></td>
                <td><%= link_to 'Destroy', print, method: :delete, data: { confirm: 'Are you sure?' } %></td>
                <td><%= link_to 'Next Stage', bump_up_path(print), method: :put %></td>
                <td><%= link_to 'Last Stage', bump_down_path(print), method: :put %></td>
              </tr>
            <% end %>
          <% end %>
        </tbody>
      </table>
      <br>
    </div>

    <!-- PickUp Orders -->
    <div id="pickup" class="col s12">
      <h1>PickUp</h1>
      <%= link_to 'New Print', new_print_path %>
      <table>
        <thead>
          <tr>
            <th>Name</th>
            <th>Quantity</th>
            <th>Order state</th>
            <th>Contact email</th>
            <th>Contact phone</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @prints.each do |print| %>
            <% if print.order_state == 2 %>
              <tr>
                <td><%= print.name %></td>
                <td><%= print.quantity %></td>
                <td><%= print.order_state %></td>
                <td><%= print.contact_email %></td>
                <td><%= print.contact_phone %></td>
                <td><%= link_to 'Show', print %></td>
                <td><%= link_to 'Edit', edit_print_path(print) %></td>
                <td><%= link_to 'Destroy', print, method: :delete, data: { confirm: 'Are you sure?' } %></td>
                <td><%= link_to 'Next Stage', bump_up_path(print), method: :put %></td>
                <td><%= link_to 'Last Stage', bump_down_path(print), method: :put %></td>
              </tr>
            <% end %>
          <% end %>
        </tbody>
      </table>
      <br>
    </div>

    <!-- Archived Orders -->
    <div id="archive" class="col s12">
      <h1>Archive</h1>
      <%= link_to 'New Print', new_print_path %>
      <table>
        <thead>
          <tr>
            <th>Name</th>
            <th>Quantity</th>
            <th>Order state</th>
            <th>Contact email</th>
            <th>Contact phone</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @prints.each do |print| %>
            <tr>
              <td><%= print.name %></td>
              <td><%= print.quantity %></td>
              <td><%= print.order_state %></td>
              <td><%= print.contact_email %></td>
              <td><%= print.contact_phone %></td>
              <td><%= link_to 'Show', print %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <br>
    </div>

  </div>
</div>
